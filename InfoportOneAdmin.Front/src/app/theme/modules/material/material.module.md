# MaterialForAngularModule

Este módulo incluye las importaciones de los módulos y los servicios, necesarios para el buen funcionamiento de la plantilla, haciendo uso de Angular Material.
Este módulo es usado en el `importProvidersFrom()` del `app.config.ts`

(Se Mantiene `MaterialForAngularModule`, para no sobrecargar el `app.config.ts`)
