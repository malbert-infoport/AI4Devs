# KendoUiModule

Este módulo incluye las importaciones de los módulos y los servicios, necesarios para el buen funcionamiento de Kendo Telerik y la integración con la `@cl/common-library`.  
Este módulo es usado en el `importProvidersFrom()` del `app.config.ts`

(Se mantiene `KendoUiModule`, para no sobrecargar el `app.config.ts`)
